apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sop.fullname" . }}-test-connection"
  labels:
    {{- include "sop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
